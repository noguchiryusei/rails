<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class=" items-center">
    <h1 class="font-bold text-4xl">Tweets</h1>
    <%= link_to "New tweet", "/tweets/new", class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="tweets" class="min-w-full">
    <%= render @tweets %>
  </div>
</div>

<div id="app">
  <button class="material-symbols-outlined"  v-on:click= "viewSearchChange()">
    {{ text }}
</button>
<div v-show="isShow">{{ text }}</div>
  </div>

<div id="vue_example"></div>
</div>
<script>

</script>